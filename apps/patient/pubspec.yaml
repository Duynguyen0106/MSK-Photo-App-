name: patient
description: MSK Suite — patient-facing app.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  msk_core:
    path: ../../packages/msk_core
  provider: ^6.1.2
  camera: ^0.11.0+2
  cupertino_icons: ^1.0.8
  path_drawing: ^1.0.1
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/body_map.svg
