name: clinician
description: MSK Suite — clinician measurement and documentation tool.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  msk_core:
    path: ../../packages/msk_core
  provider: ^6.1.2
  camera: ^0.11.0+2
  fl_chart: ^0.69.0
  interactive_3d: ^1.2.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
