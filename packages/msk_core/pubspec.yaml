name: msk_core
description: Shared services for MSK Suite — no UI.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_pose_detection: ^0.14.0
  camera: ^0.11.0+2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  pdf: ^3.11.1
  printing: ^5.13.4
  path_provider: ^2.1.4
  provider: ^6.1.2
  fl_chart: ^0.69.0
  shared_preferences: ^2.3.2
  image: ^4.2.0
  flutter_secure_storage: ^9.2.2
  csv: ^6.0.0
  archive: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
